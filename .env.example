PORT=8080
DB_PATH=./data/gestionale.db
OPENCITY_BASE_URL=https://service.comune.montesilvano.pe.it
SECRET_KEY=<genera con: openssl rand -hex 32>
ADMIN_USERNAMES=apioperator
TRUST_PROXY=false
# Credenziali per cmd/batch (fetch istanze + CSV/HTML)
OPENCITY_USERNAME=apioperator
OPENCITY_PASSWORD=<password OpenCity>
# DEV=true abilita GET /dev/reload-templates (non usare in produzione)
# DEV=true
